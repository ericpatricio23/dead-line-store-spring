services:
  api:
    build: .
    ports:
      - "8080:8080"
    environment:
      - SPRING_DATASOURCE_URL=jdbc:sqlite:/app/data/loja.db
      - JWT_SECRET=troque-por-uma-chave-forte
    volumes:
      - sqlite-data:/app/data
    restart: unless-stopped

volumes:
  sqlite-data: